# Copy this file to .env (gitignored) - docker compose reads it automatically.
# Defaults in docker-compose.yml already match the shared postgres_main
# container this machine runs; override only if your Postgres instance uses
# different credentials or a different database name.
DB_URL=jdbc:postgresql://host.docker.internal:5432/spring_graphql_tutorial
DB_USERNAME=admin
DB_PASSWORD=admin
